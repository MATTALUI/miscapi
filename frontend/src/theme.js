// Currently this file isn't actually being used. It was causing me errors because
// I am a dummy. I'm not getting rid of it though, because it would be nice to have
// universal styles referenced somewhere so I don't mess up the blues.

const appTheme = {
  spacing: (count) => `${count}rem`,
  palette: {},
  colors: {
    // Themes
    primary: "#3587A4",
    secondary: "#88CCF1",
    terciary: "",
    dark: "#1C3144",
    light: "#C1DFF0",
    // Colors
    red: "#D00000",
    white: "#E8E9F3",
    oranhe: "#F18F01"
  }
};

export default appTheme;
